import PublicSectorContent from "./PublicSectorContent";

export const metadata = {
    title: 'Public Sector & Healthcare Security | SSG',
    description: 'Security services for government, education, and healthcare institutions. Protecting the public and critical infrastructure.',
};

export default function PublicSectorPage() {
    return <PublicSectorContent />;
}
